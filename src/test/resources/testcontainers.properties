# Testcontainers configuration for CI environments
# Disable Ryuk resource reaper checks which can be flaky in CI
testcontainers.reuse.enable=false
# Use smaller timeouts for startup
checks.disable=false
